import m from "mithril";

const Profile = {
  view: vnode => (
    <section>
      <div>
        <h1>My Profile</h1>
      </div>
      <small>This is where my profile dashboard goes...</small>
    </section>
  )
};

export default Profile;
